require(['checkInput', 'api'], function(reg, api) { 
    reg.init();
} )

require(['eyesStatus'], function(eyes) {
    eyes.init();
} )

// require(['validationCode'], function(code) {
//     code.init();
// } )
